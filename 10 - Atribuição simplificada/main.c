#include <stdio.h>
#include <stdlib.h>

int main() {
  // Sem atribuição simplificada
  // int x = 10;
  // int y = 10;

  // x = x + y;

  // Com atribuição simplificada
  int x = 12;
  int y = 10;

  // x *= y; -> x = x * y
  // x += y; -> x = x + y
  // x /= y; -> x = x / y
  // x %= y; -> x = x % y

  printf("%d", x);

  return 0;
}