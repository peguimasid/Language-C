#include <stdio.h>
#include <stdlib.h>

int main() {
  int x = 10;
  int y = 10;

  printf("%d\n", x == y);  // 1
  printf("%d", x > y);     // 0

  // 0: operação é falsa (false)
  // 1: operação é verdadeira (true)

  // Operadores:
  // >
  // >=
  // <
  // <=
  // ==
  // !=

  return 0;
}